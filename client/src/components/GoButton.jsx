import React, {Component, PureComponent} from "react";

export default class GoButton extends Component {
	render() {
		return(
			<p>GoButton component. Not sure if this should be a separate component.</p>
		)
	}
}