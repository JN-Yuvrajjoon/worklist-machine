import React, {Component} from "react";

// Won't delete this yet; may serve a purpose
// WorklistNavbar component currently has two very separate functions:
// Input (page flipping), and
// Output (Variations of the worklist, warnings, scoring)

// Variations might be a bit of both, we'll see
export default class WorklistInformationBar extends Component{
  render() {
    return (
      <p>WorklistInformationBar component</p>
    )
  }
}